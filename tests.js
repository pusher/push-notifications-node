const expect = require('chai').expect;

describe('PushNotifications Node SDK', () => {
    describe('Constructor', () => {
        it('should fail', () => {
            expect(1).to.equal(0);
        });
    });
});
