language: node_js
node_js:
- "8.5"
cache:
  yarn: true
install: yarn install
jobs:
  include:
    - stage: test
      name: "Integration tests"
      script: yarn test:integration
    - script: yarn test:unit
      name: "Unit tests"
    - script: yarn lint
      name: "Lint & Format"
